const { Router } = require('expresss');

const route = Router();

route.get("/", (req, res)=>{});

route.post("/", (req, res)=>{});

route.put("/", (req, res)=>{});

route.delete("/", (req, res)=>{});